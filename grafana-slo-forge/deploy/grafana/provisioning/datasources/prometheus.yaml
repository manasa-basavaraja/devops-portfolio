apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: DS_PROMETHEUS
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
