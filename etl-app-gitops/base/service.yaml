apiVersion: v1
kind: Service
metadata:
  name: etl-app
  labels:
    app: etl-app
spec:
  type: ClusterIP
  selector:
    app: etl-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000